version: '3.8'

services:
  p2p-node:
    build: .
    ports:
      - "4001:4001"
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - p2p-network

networks:
  p2p-network:
    driver: bridge